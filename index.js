exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('creating lambda from github test1'),
    };
    return response;
};
